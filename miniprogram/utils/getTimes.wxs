var handelDate = {
  getTimes:function (val){ 
    if(val) {
      return getDate(val).getTime()
    } else {
      return getDate().getTime()
    }
  }
}
module.exports = {
  getTimes: handelDate.getTimes
}